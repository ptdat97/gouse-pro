# Địa chỉ Go backend.
#
# Chạy cùng máy khi phát triển: cd ../gouse && make run
NEXT_PUBLIC_API_URL=http://localhost:8080

# Backend phải cho phép CẢ HAI origin khi phát triển:
#   admin      http://localhost:3000
#   cửa hàng   http://localhost:3001
# Đây là mặc định của Go backend ở môi trường development — chỉ cần đặt
# AUTH_ALLOWED_ORIGINS khi chạy ở cổng khác.
